apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-ingress
  labels:
    project: k8s
spec:
  defaultBackend:
    service:
      name: django-service
      port:
        number: 80